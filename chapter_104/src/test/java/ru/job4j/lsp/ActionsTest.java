package ru.job4j.lsp;

import org.junit.Before;
import org.junit.Test;

import java.io.BufferedReader;
import java.io.PrintWriter;
import java.io.StringReader;
import java.io.StringWriter;
import java.util.LinkedHashMap;

import static org.hamcrest.core.Is.is;
import static org.junit.Assert.assertThat;

public class ActionsTest {
/*
    Parking park = new Parking();
    Car car = new Car("X444XX");
    StringWriter out = new StringWriter();
    PrintWriter output = new PrintWriter(out);

    @Before
    public void start() {
        LinkedHashMap<String, Car> pk = new LinkedHashMap<>();
        pk.put("1", null);
        pk.put("2", null);
        park.setParking(pk);
        output.toString();
    }

    @Test
    public void addCarByAction () {
        BufferedReader input = new BufferedReader(new StringReader("X444XX%nn%n"));
        ParkCar parkCar = new ParkCar(input, output);
        String exp = String.format("Input car number: %nCaro(y/n): %nParked to place# 1%n");
        assertThat(parkCar.execute(park), is (true));
        output.flush();
        assertThat(out.toString(), is (exp));
    }

    @Test
    public void getCarByAction () {
        BufferedReader input = new BufferedReader(new StringReader ("X444XX%n"));
        GetCar getCar = new ParkCar(input, output);
        park.add(car);
        String exp = String.format("Input car number: %nOK%n");
        assertThat(getCar.execute(park), is (true));
        output.flush();
        assertThat(out.toString(), is (exp));
    }

 */
}
