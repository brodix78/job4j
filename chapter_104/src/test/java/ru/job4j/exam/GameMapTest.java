package ru.job4j.exam;

import org.junit.Test;
import java.util.List;
import static org.hamcrest.Matchers.is;
import static org.junit.Assert.assertThat;

public class GameMapTest {
/*
    GameMap map = new SomeGameMap(List.of(new Cell(0, 0),
            new Cell(0, 0),
            new Cell(0, 0),
            new Cell(-1, 0),
            new Cell(0, 2),
            new Cell(0, 0),
            new Cell(0, 0),
            new Cell(0, 0)
    ));

    @Test
    public void whenMakeTwoStepsWithoutExtra() {
        Player exp = new Player("Somebody");
        exp.makeSteps(2);
        assertThat(map.makeMove(new Player("Somebody"), 2), is(exp));
    }

    @Test
    public void whenMakeFourStepsAndLostOneMove() {
        Player exp = new Player("Somebody");
        exp.makeSteps(4);
        exp.addMoves(-1);
        assertThat(map.makeMove(new Player("Somebody"), 4), is(exp));
    }

    @Test
    public void whenMakeFiveStepsWhenGiveTwoExtraStepsForwardToSevenCell() {
        Player exp = new Player("Somebody");
        exp.makeSteps(7);
        assertThat(map.makeMove(new Player("Somebody"), 5), is(exp));
    }

    @Test
    public void whenMadeNineStepsAndWin() {
        Player exp = new Player("Somebody");
        exp.makeSteps(9);
        exp.won;
        assertThat(map.makeMove(new Player("Somebody"), 9), is(exp));
    }*/
}
